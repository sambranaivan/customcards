-- Lick
local s,id=GetID()
function s.initial_effect(c)
    -- TODO: Programar efecto
    -- Activa cuando el oponente declara un ataque: el monstruo atacante pierde 700 ATK hasta el final del turno. Si su ATK llega a 0 o menos por este efecto: el ataque se cancela y ese monstruo no puede atacar el resto del turno. Si tienes un monstruo [Gengar] en tu GY: en lugar de cancelar el ataque, puedes hacer que el monstruo atacante ataque directamente a su controlador con su ATK reducida. *(Lick redirige el golpe — el atacante termina golpeándose a sí mismo)*
end
